import OpenDentalPage from '@/components/OpenDentalPage';

export const metadata = {
  title: 'AI Receptionist for Open Dental | OrapexAI',
  description: 'OrapexAI connects directly to Open Dental in real-time. Books appointments, verifies patients, checks insurance — all during the live call. HIPAA compliant.',
};

export default function Page() {
  return <OpenDentalPage />;
}
