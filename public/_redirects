# OrapexAI — Cloudflare Pages redirect rules
# This file goes in public/ so it lands in dist/ after build.
#
# The /* → /index.html rule makes Cloudflare serve your React app
# for every URL, so direct navigation to /pricing etc. works.
# WITHOUT this, going directly to orapexai.com/pricing returns 404.
# With prerender.mjs, each route has its own index.html, so this
# is a fallback for any routes not pre-rendered.

/*    /index.html   200
